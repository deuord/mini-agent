# mini-agent
A lightweight desktop task agent — understands natural language, operates files, runs commands and processes documents via ReAct loop + function calling (Python, LLM-powered).

> **mini-agent：轻量级桌面任务 Agent**  一个能听懂自然语言指令的桌面助手：自主规划任务步骤，操作文件（查看/读取/编辑/创建）、执行命令（带安全确认）、处理文档（CSV/Excel/Word）。基于 **ReAct 循环（推理→行动→观察）** 与 **Function
